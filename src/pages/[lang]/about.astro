---
import Playlist from '../../components/playlist.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';

export function getStaticPaths() {
  return [
    {params: {lang: 'en'}},
    {params: {lang: 'br'}},
    {params: {lang: 'fr'}},
    {params: {lang: 'es'}},
  ];
}

const { lang } = Astro.params;
---
<BaseLayout>
  <article>
      <Playlist />
  </article>
</BaseLayout>

// TODO: create language switching mechanism
// TODO: build spanish, french and english full versions
// TODO: fix responsiveness to small screens
// TODO: carousel with highlighted projects